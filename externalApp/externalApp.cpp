#include <iostream>
#include "FileSystemClass.h"
#include "Header.h"
#include "pch.h"
#include <stdio.h>
#include <windows.h>

int main()
{
	if (getDevName)
	{
		getDevName(); 
	}
	else
	{
		perror("Error: ");
	}

	system("pause");
	return 0;
}
